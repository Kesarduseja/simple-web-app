name: Docker CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: cloudapp123
        password: KESAR@0508

    - name: Build and Push
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: cloudapp123/ca-web-app:latest
